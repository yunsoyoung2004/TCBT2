# 인수인계서 — TBCT S01~S03 충실도(fidelity) 작업

- 작성일: 2026-08-15 (작업은 2026-08-13 밤 ~ 2026-08-15 새벽 사이 연속 세션)
- 브랜치: `Session1-3` / 마지막 베이스 커밋: `8805f75`
- **커밋/푸시 없음** — 모든 변경은 아직 워킹트리에만 있는 미커밋 상태입니다. 확인 후 원하시는 단위로 커밋해 주세요.
- 이 문서는 "최초 git pull 시점 → 지금"까지 무엇이, 왜 바뀌었는지를 한 번에 파악하도록 정리한 것입니다. 코드 자체의 상세 아키텍처는 `docs/ai/TBCT_SESSIONS_1_3.md`(2026-08-13자 검증 노트, 이번 작업으로 갱신하지 않음)와 `docs/ai/CODEMAP.md`를 참고하세요.

---

## 1. 무엇을 요청받았나 (작업 순서대로)

1. **Phase 0 조사** — `docs/TBCT_S01-S03_고도화_기획안_v3.md` 스펙에 따라 코드만 읽고 보고 (수정 없음) → 승인 대기
2. **Phase 1 수정** — 승인된 6개 파일 범위 내에서 실제 수정 + 검증
3. 우울증 환자 시나리오 대본 작성 → S01~S03 실행 → 로그 파일 저장
4. 터미널에서 직접 환자 역할로 대화할 수 있는 로컬 실행 스크립트 작성 (Claude API 있으면 사용, 없으면 결정론적 폴백)
5. `[나]`/`[환자]` 라벨 중복 출력 버그 조사 → 프로덕션(Vercel) 영향 여부 확인 → 로컬 스크립트를 정상적인 핑퐁 대화처럼 수정
6. (Vercel 프리뷰 요청 → 계정 없음 확인 → 로컬 전용으로 방향 전환)
7. API 키를 터미널에 노출해도 되는지 질문 → `.env.local` 방식 안내 및 자동 로드 구현
8. **"정적분석 통합 리포트"(P0/P1/TEST 판정 문서) 검토** — 각 항목을 실제 코드와 대조해 진위 판정 → 확인된 실제 결함만 수정
9. **"S02/S03 로그 수정 기획안" 문서 처리** — §9-5(D-11) 실증 조사(코드 정적 분석이 아니라 실제 세션을 돌려서 확인) 후 보고 → 승인 대기 → Phase 1~5 진행
10. **자율 작업 지시** ("제공 자료 중 수정 가능하다면 최대한 수정 수행해... 폴백 꼭 정상 작동할 수 있도록 확인하고") — 사용자가 제공한 수정 권한 범위표 내에서 최대한 커버리지를 넓히고, 폴백 메커니즘이 실제로 동작하는지 실증적으로 검증
11. **이 인수인계서 작성** (지금 이 문서)

---

## 2. 실제로 바뀐 파일 (git diff 기준)

```
 .gitignore                                          |   8 +-   (*.log 무시 추가 등, 사소)
 src/lib/dialogue-agent/dialogue-agent-stale-value.test.ts | 16 +-
 src/lib/protocol/sessions/s01.ts                    | 257 +++++++++++++++++----
 src/lib/protocol/sessions/s02.ts                    | 108 ++++++---
 src/lib/protocol/sessions/s03.ts                    | 243 ++++++++++++++++---
 src/lib/runtime/runtime-context.ts                  |  15 ++
 src/lib/runtime/runtime-static-message.ts            |   6 +-
 src/lib/runtime/static-messages/s01.ts               |  83 +++++--
 src/lib/runtime/static-messages/s02.ts               | 121 ++++++++--
 src/lib/runtime/static-messages/s03.ts               | 162 ++++++++++++-
 11 files changed, 864 insertions(+), 157 deletions(-)
```

새로 추가된(미추적) 파일:
- `.claude/TASK_SCOPE.json` — 이번 작업의 수정 허용 범위 기록 (아래 §6 참고)
- `CLAUDE.md`, `docs/`, `prompts/` — 프로젝트 운영 규칙/매뉴얼류 (이번 세션 이전에 이미 준비되어 있던 온보딩 자료; 코드 변경물이 아님)
- `scripts/run-local-session.ts` — 로컬 터미널 대화형 세션 러너 (요청 4, 5, 7)
- `scripts/run-depressed-scenario.ts` — 우울증 환자 시나리오 오프라인 러너 (요청 3)

**수정 허용 범위는 처음부터 끝까지 8개 파일 + 이번에 추가된 2개 스크립트로 고정되어 있었고, 그 밖의 파일(공용 엔진, S04~S08, DB 스키마 등)은 손대지 않았습니다.**

---

## 3. 발견하고 고친 핵심 버그 (영향도 순)

### 3-1. 불리언(boolean) 프롬프트가 정답을 줘도 영원히 재질문하는 버그 — `runtime-context.ts`
- **증상**: S03의 새 `redirection-contract` 프롬프트(`validation:{kind:"boolean"}`)에 "네"라고 답해도 계속 "insufficient_input"으로 되돌아가 절대 진행되지 않음.
- **원인**: `isMeaningfulTextResponse()`가 `NON_ANSWER_TEXT`(무응답으로 간주할 문구 블랙리스트)를 boolean/enum 수용 검사보다 **먼저** 체크하고 있었는데, 이 블랙리스트 안에 `"yes"`, `"no"`, `"네"`, `"예"`, `"응"` 등이 그대로 들어 있었습니다. 즉 boolean 프롬프트의 정답 자체가 "무응답"으로 오분류됨.
- **영향 범위**: 이번에 추가한 `redirection-contract` 하나만이 아니라, **코드베이스 전체의 모든 boolean-kind 프롬프트**(S01 `confirm-list` 등)가 이 잠재적 결함을 갖고 있었습니다. 다만 지금까지는 이 정확한 경로(자유 텍스트로 boolean 답을 타이핑)가 실제로 시험된 적이 없어서 드러나지 않았던 것으로 보입니다.
- **수정**: `evidenceFor`/`evidenceAgainst` 가드 직후, 블랙리스트 체크보다 앞에 `validation.kind === "boolean" | "enum"`이면 바로 수락 여부를 판정하는 early return을 추가. 사용자에게 `AskUserQuestion`으로 명시적 승인 받고 진행.
- **검증**: 수정 전/후로 실제 세션을 돌려서 확인 — 수정 전엔 `redirection-contract`에서 영원히 멈춤, 수정 후엔 정상적으로 `describe-situation`으로 진행되고 한국어 텍스트도 정상 출력됨.

### 3-2. S03 Q10 결론 확신도(conclusion-belief) 이중 질문 버그 — 제가 Phase 1에서 만든 회귀 버그, 이번에 재발견해 수정
- **증상**: `full-conclusion-readback`이 결론 되읽기 + 확신도 평점 질문을 동시에 물어서, 답변이 엉뚱하게 `conclusionReadBackComplete`(확인 플래그)에 저장되고, 바로 다음 `conclusion-belief`가 사실상 똑같은 질문을 또 물음.
- **수정**: `src/lib/runtime/static-messages/s03.ts`에서 두 프롬프트의 정적 텍스트를 명확히 분리 (되읽기+확인만 하는 것과, 0~100% 확신도만 묻는 것).
- **검증**: 시나리오 R-10 재실행 → 중복 질문 없이 두 턴으로 깔끔하게 분리됨을 확인.

### 3-3. `returningParticipant` 죽은 필드 버그 — S02
- **증상**: `first-session-opening`의 `activationCondition`이 `{field:"returningParticipant", operator:"equals", value:false}`였는데, 이 필드는 신규 세션에서 절대 설정되지 않아 항상 `undefined`. `undefined === false`는 `false`이므로 **매 세션마다 오프닝이 스킵됨.**
- **수정**: `not_equals: true`로 변경 (필드가 없거나 false인 모든 경우에 오프닝이 나가도록).

### 3-4. 세 후보(candidate-two/three) 감정 재확인 단계의 필드 오염 — S01
- **증상**: "가능성을 상상할 수 있나요?"라는 재확인 질문의 자유 텍스트 답변이 그대로 `candidateTwoEmotion`/`candidateThreeEmotion`에 저장되어, "네" 같은 답이 감정값으로 들어가는 문제.
- **수정**: 답변은 별도 필드(`candidateTwoPossibilityAccepted` 등)에 저장하고, `completionEffect:{type:"set_field", ...}`로 감정값은 결정론적으로 고정.

이 4가지가 **동작(behavior)에 실제 영향을 준** 버그이며, 나머지는 아래 §4의 "본문 복원" 작업입니다.

---

## 4. 본문(patientText/koreanText) 복원 작업 — 무엇을, 왜

세 세션 모두에서, "승인된 고정 문구가 없는 프롬프트는 영어 원문이 없으면 일반적인 중립 문구(generic fallback)로 대체된다"는 원칙이 있습니다(`resolveLocaleFallbackPatientText`). 이번 세션 이전에는 다수의 프롬프트에 `patientText`(영어 원문)와 `koreanText`(한국어 대응문)가 비어 있어서, 실제로는 "일반 문구로 대체될 위험"이 있는 상태였습니다.

- **S01**: 신규/부족했던 17개 항목에 `tbct-source-text.generated.ts`의 원문을 그대로(verbatim) 복원 + 한국어 번역 17건 추가. (`three-person-observation`, `read-distortions`, `outcome-gap` 등)
- **S02**: 오프닝 4개, 문제 수집 7개, 숨은 문제 2개, 척도 안내류, 목표 수집 7개 등 총 29건 영/한 텍스트 추가.
- **S03**: safety-check, 14문항 인트로, Q1~Q14 전 구간, 클로징까지 약 28건 영/한 텍스트 추가.
- 모든 곳에서 **arm-neutral 표현**("your therapist" → "담당 연구 임상의"/"study clinician")을 일관되게 적용 — 이 프로토콜(V9 Arm 3)은 AI 단독 진행이라 배정된 치료자가 없기 때문입니다.

이 작업은 "임상 내용을 새로 창작"한 것이 아니라, **이미 소스 매뉴얼에 있는 원문을 그대로 옮긴 것**입니다 (`Never hand-edit tbct-source-text.generated.ts` 규칙은 지켰습니다 — 그 파일은 읽기만 하고 한 번도 수정하지 않았습니다).

---

## 5. 검증 결과

### 5-1. 정적 검증
- `npm run type-check` — **통과** (에러 0건)
- `npm test` — **35개 테스트 파일, 163개 테스트 전부 통과**

### 5-2. "폴백이 실제로 동작하는가" 실증 검증 (사용자가 명시적으로 요청한 부분)
가장 신뢰도 높은 방법으로, 제가 만든 정적 분석 하네스가 아니라 **실제 프로덕션 API 진입점**(`runtime-session-api.ts`/`runtime-execution-api.ts`)을 그대로 호출하는 오프라인 스크립트로 S01/S02/S03을 각각 한국어(`ko-KR`) 로케일로 **끝까지(session status "completed"까지)** 실행했습니다.

| 세션 | 턴 수 | 일반 폴백(generic fallback) 노출 | 미치환 `[bracket]` 잔존 | 영어 텍스트 유출 |
| --- | --- | --- | --- | --- |
| S01 | 34턴 | 0건 | 0건 | 0건 |
| S02 | 29턴 | 0건 | 0건 | 0건 |
| S03 | 30턴 | 0건 | 0건 | 0건 |

**결론: 세 세션 모두 한국어 세션을 처음부터 끝까지 진행해도 일반 폴백 문구나 영어 텍스트가 새어 나오지 않습니다.** §3-1의 boolean 버그가 이 검증을 사실상 가로막고 있었기 때문에, 그 버그를 고치기 전에는 이 방식의 검증 자체가 불가능했습니다.

### 5-3. 로컬 회귀 하네스 (`tests/tbct-phase1/`, git에는 포함되지 않음 — `.git/info/exclude`)
이전 세션에서 사용자 스펙에 따라 만든 정적+시나리오 검증 스크립트(T01~T16, R-01~R-14)를 재실행했습니다. 이번 S01 작업 직전 스냅샷과 비교한 결과:
- **개선된 항목**: T16(핵심 프롬프트 문구 미정의 위험) 7건 실패 → **0건**(통과). R-14.1 S01/한국어 완주 검사 실패 → **통과**.
- **변동 없음(이번 작업 범위 밖, 기존부터 있던 항목)**: S02/S03 관련 시나리오 몇 건(R-04~R-12 일부), T05/T06/T11.

이 하네스 자체에서 **T02(한/영 키 동등성) 13건, T03(placeholder 누출) 8건, R-14.3 S02/한국어 1건**이 계속 "실패"로 나오는데, 직접 코드를 추적한 결과 다음과 같이 **전부 실제 제품 결함이 아니라 제 검증 스크립트 자체의 한계**로 확인했습니다:
- T02/T03: 이 검사 스크립트는 `resolveStaticText()`를 로케일 없이 호출하고, `resolveBracketPlaceholders()`(대괄호 치환)를 아예 거치지 않습니다. 실제 런타임은 이 두 단계를 모두 거치므로 문제 없음 (§5-2에서 실증 확인).
- R-14.3: `tests/tbct-phase1/`가 세션 스펙 파일과 정적 메시지 파일을 동시에 여러 경로로 import하면서, 그 프로세스 안에서만 `REVIEWED_KOREAN_PROMPT_TEXT`(한국어 사전)에 S02 항목이 비어 보이는 모듈 로딩 순서 문제를 발견했습니다. 동일한 세션을 정식 API로 직접 실행하면(§5-2) 문제없이 한국어가 나옵니다. **로컬 검증 스크립트만의 문제이며 실제 서비스에는 영향 없음.**
- T05(3인 예시 문구 동일성 검사), T06(distortion 개수 강제 해제 검사): 정규식이 아포스트로피(`'`)와 스마트 따옴표(`'`)를 혼동하거나, 코드 주석에 이미 명시된 설계 변경(읽기 개수 ≠ 선택 개수)을 검사 스크립트가 못 따라온 것으로 확인 — 코드는 의도대로 동작 중입니다.

---

## 6. 알려진 한계 / 다음에 볼 사람이 알아야 할 것

1. **Enum(선택형) 프롬프트의 답변 버튼은 여전히 영어입니다.** (예: S03 Q14 `global-evaluation`의 "same / a little better / much better" 버튼) 질문 문장 자체는 한국어로 잘 나가지만, 클릭형 답변 버튼 라벨은 `patient-input-controls.tsx`가 enum의 원본 영어 값을 그대로 라벨로 씁니다. 이 파일은 사용자가 제공한 권한표에서 **"공용파일·상담 필요"**로 명시적으로 제외된 파일이라 이번에는 손대지 않았습니다. 한국어 세션에서 영어 버튼이 섞여 보이는 것 자체는 실제 UX 이슈이니, 다음에 손볼 때 이 파일부터 논의해 주세요.
2. **로컬 회귀 하네스(`tests/tbct-phase1/`)의 R-14.3/T02/T03 실패는 재확인 없이 무시해도 됩니다** (§5-3에서 근본 원인까지 확인함) — 다만 이 하네스 자체를 고칠 계획이면 "여러 경로로 같은 파일을 import할 때 모듈이 중복 로드되는 문제"부터 봐야 합니다.
3. **S02/S03 시나리오 회귀(R-04, R-05, R-06, R-08, R-09, R-10, R-12) 중 일부는 여전히 실패 상태**입니다. 코드를 확인한 결과 이번 S01 17건 작업과는 무관한, 그 이전부터 있던 별개의 항목들입니다 (예: R-06은 시나리오 스크립트 자체가 "seed 주입 미지원"이라 의도적으로 에러를 던지도록 짜여 있음). 시간 관계상 이번 세션에서는 깊이 파고들지 않았습니다 — 다음 작업 후보로 남겨둡니다.
4. **커밋되지 않았습니다.** `git status`로 위 §2의 변경 사항을 확인한 뒤 원하는 단위로 나눠서 커밋해 주세요.

---

## 7. 로컬에서 직접 확인하는 방법

```bash
# 1) 타입체크 + 테스트
npm run type-check
npm test

# 2) 터미널에서 직접 환자 역할로 대화 (Claude API 키 있으면 실제 phrasing, 없어도 결정론적 폴백으로 진행됨)
#    .env.local에 ANTHROPIC_API_KEY=... 를 넣어두면 자동으로 읽습니다 (커밋되지 않음, .gitignore 처리됨)
npx tsx scripts/run-local-session.ts tbct-s01 --interactive          # 한국어
npx tsx scripts/run-local-session.ts tbct-s02 --interactive --en     # 영어
npx tsx scripts/run-local-session.ts tbct-s03                        # 자동 재생(합성 답변)

# 3) 우울증 환자 시나리오 오프라인 재생
npx tsx scripts/run-depressed-scenario.ts
```

---

## 8. 참고 문서

- `docs/ai/TBCT_SESSIONS_1_3.md` — 세션별 아키텍처 검증 노트 (2026-08-13 기준, 이번 세션에서 갱신하지 않음 — 이번에 고친 버그들을 반영하려면 별도로 업데이트 필요)
- `docs/ai/CODEMAP.md` — 코드베이스 전체 내비게이션
- `.claude/TASK_SCOPE.json` — 이번 작업의 파일별 허용 범위와 각 예외 승인 근거
- `logs/tbct-phase1/` — 로컬 회귀 하네스 실행 로그 (git에는 포함되지 않음, `.git/info/exclude` 처리)
